name: Build NaiveProxy for HarmonyOS

on:
  push:
    branches: [master, ohos-*]
  pull_request:
    branches: [master]
  workflow_dispatch:
    inputs:
      arch:
        description: '目标架构'
        required: false
        default: 'arm64'
        type: choice
        options:
          - arm64
          - x64
          - both

defaults:
  run:
    shell: bash

env:
  CACHE_EPOCH: 1

jobs:
  build-ohos-so:
    name: 编译鸿蒙 .so 库 (${{ matrix.arch }})
    runs-on: ubuntu-22.04

    container:
      image: simtelboyhotyi/ohos-build:latest
      options: --privileged

    strategy:
      fail-fast: false
      matrix:
        arch: ${{ github.event.inputs.arch == 'both' && fromJSON('["arm64", "x64"]') || github.event.inputs.arch == '' && fromJSON('["arm64"]') || fromJSON(format('["{0}"]', github.event.inputs.arch)) }}

    steps:
      - name: Checkout 代码
        uses: actions/checkout@v4

      - name: 验证鸿蒙 NDK sysroot
        run: |
          echo "========================================="
          echo "验证鸿蒙 NDK sysroot"
          echo "========================================="
          echo "NDK 路径: $OHOS_NDK_HOME"
          echo "Sysroot 路径: $OHOS_SYSROOT"
          ls -la $OHOS_SYSROOT/usr/lib
          echo ""
          echo "✅ 鸿蒙 sysroot 已就绪！"

      - name: 缓存编译工具链
        uses: actions/cache@v4
        with:
          path: |
            src/third_party/llvm-build/Release+Asserts/
            src/gn/
          key: toolchains-ohos-${{ hashFiles('CHROMIUM_VERSION') }}-v${{ env.CACHE_EPOCH }}

      - name: 缓存 ccache
        uses: actions/cache@v4
        with:
          path: ~/.cache/ccache
          key: ccache-ohos-${{ matrix.arch }}-${{ hashFiles('CHROMIUM_VERSION') }}-${{ github.run_number }}
          restore-keys: |
            ccache-ohos-${{ matrix.arch }}-${{ hashFiles('CHROMIUM_VERSION') }}-

      - name: 修改构建配置支持鸿蒙
        working-directory: src
        run: |
          echo "========================================="
          echo "修改 GN 配置以支持鸿蒙平台"
          echo "========================================="

          # 1. 修改 get-sysroot.sh 添加鸿蒙支持
          if ! grep -q "ohos)" get-sysroot.sh; then
            sed -i '/android)/a\  ohos)\n    WITH_SYSROOT=\n  ;;' get-sysroot.sh
          fi

          echo "配置修改完成"

      - name: 修改 BUILD.gn 排除 tracing
        working-directory: src
        run: |
          echo "========================================="
          echo "修改 base/BUILD.gn 排除 tracing 和 Perfetto 依赖"
          echo "========================================="

          # 备份原文件
          cp base/BUILD.gn base/BUILD.gn.bak

          # 注释掉所有 Perfetto 相关的依赖
          sed -i 's/"\/\/base\/tracing\/protos:chrome_track_event",/# "\/\/base\/tracing\/protos:chrome_track_event",/' base/BUILD.gn
          sed -i 's/"\/\/base\/tracing\/protos:chrome_track_event_zero",/# "\/\/base\/tracing\/protos:chrome_track_event_zero",/' base/BUILD.gn
          sed -i 's/"\/\/third_party\/perfetto:libperfetto",/# "\/\/third_party\/perfetto:libperfetto",/' base/BUILD.gn
          sed -i 's/"\/\/third_party\/perfetto\/include\/perfetto\/protozero",/# "\/\/third_party\/perfetto\/include\/perfetto\/protozero",/' base/BUILD.gn
          sed -i 's/all_dependent_configs += \[ "\/\/third_party\/perfetto\/gn:public_config" \]/# all_dependent_configs += [ "\/\/third_party\/perfetto\/gn:public_config" ]/' base/BUILD.gn

          echo "✅ base/BUILD.gn 修改完成"

      - name: 修改 BUILD.gn 添加共享库目标
        working-directory: src
        run: |
          echo "========================================="
          echo "添加 shared_library 编译目标"
          echo "========================================="

          # 在 net/BUILD.gn 中添加共享库目标
          cat >> net/BUILD.gn <<'EOFBUILDGN'

          # 鸿蒙共享库版本
          if (target_os == "ohos" || target_os == "android") {
            shared_library("naive_shared") {
              output_name = "naive"

              sources = [
                "tools/naive/http_proxy_server_socket.cc",
                "tools/naive/http_proxy_server_socket.h",
                "tools/naive/naive_config.cc",
                "tools/naive/naive_config.h",
                "tools/naive/naive_connection.cc",
                "tools/naive/naive_connection.h",
                "tools/naive/naive_padding_framer.cc",
                "tools/naive/naive_padding_framer.h",
                "tools/naive/naive_padding_socket.cc",
                "tools/naive/naive_padding_socket.h",
                "tools/naive/naive_protocol.cc",
                "tools/naive/naive_protocol.h",
                "tools/naive/naive_proxy.cc",
                "tools/naive/naive_proxy.h",
                "tools/naive/naive_proxy_bin.cc",
                "tools/naive/naive_proxy_delegate.cc",
                "tools/naive/naive_proxy_delegate.h",
                "tools/naive/redirect_resolver.cc",
                "tools/naive/redirect_resolver.h",
                "tools/naive/socks5_server_socket.cc",
                "tools/naive/socks5_server_socket.h",
              ]

              deps = [
                ":net",
                "//base",
              ]

              configs -= [ "//build/config/gcc:symbol_visibility_hidden" ]
              configs += [ "//build/config/gcc:symbol_visibility_default" ]
            }
          }
          EOFBUILDGN

          echo "BUILD.gn 修改完成"

      - name: 获取 Chromium 编译工具链
        working-directory: src
        run: |
          echo "========================================="
          echo "获取 Chromium 编译工具链"
          echo "========================================="

          # 使用 Chromium 的 clang，不是华为的
          export EXTRA_FLAGS='target_os="android" target_cpu="${{ matrix.arch }}"'
          ./get-clang.sh

          echo "✅ Chromium 编译工具链准备完成"

      - name: 获取 GN 构建工具
        working-directory: src
        run: |
          echo "========================================="
          echo "获取 GN 构建工具"
          echo "========================================="

          # 下载预编译的 GN
          if [ ! -f gn/out/gn ]; then
            mkdir -p gn/out
            wget -O gn.zip https://chrome-infra-packages.appspot.com/dl/gn/gn/linux-amd64/+/latest
            unzip -o gn.zip -d gn/out
            chmod +x gn/out/gn
            rm gn.zip
          fi

          echo "GN 版本:"
          ./gn/out/gn --version

          echo "✅ GN 构建工具准备完成"

      - name: 创建 GN 配置文件
        working-directory: src
        run: |
          echo "========================================="
          echo "创建 GN 配置文件"
          echo "========================================="

          # 创建目录结构
          mkdir -p gn/standalone/libc++
          mkdir -p gn/standalone/sanitizers
          mkdir -p gn/standalone/toolchain

          # 创建 android.gni（导入已有的 Android 配置）
          cat > gn/standalone/android.gni <<'EOFANDROID'
          # Android configuration for OHOS build
          # Import the existing Android config which already defines these variables
          import("//build/config/android/config.gni")
          EOFANDROID

          # 创建 libc++.gni（导入已有的 c++ 配置）
          cat > gn/standalone/libc++/libc++.gni <<'EOFLIBCXX'
          # libc++ configuration for OHOS build
          # Import the existing c++ config which already defines use_custom_libcxx
          import("//build/config/c++/c++.gni")
          EOFLIBCXX

          # 创建 sanitizers/vars.gni（导入已有的 sanitizers 配置）
          cat > gn/standalone/sanitizers/vars.gni <<'EOFSANITIZERS'
          # Sanitizers configuration for OHOS build
          # Import the existing sanitizers config which already defines these variables
          import("//build/config/sanitizers/sanitizers.gni")
          EOFSANITIZERS

          # 创建 toolchain/llvm.gni
          cat > gn/standalone/toolchain/llvm.gni <<'EOFLLVM'
          # LLVM toolchain configuration for OHOS build
          declare_args() {
            # Path to LLVM build directory
            llvm_build_dir = ""
          }

          # Define is_cross_compiling for perfetto
          # This is true when target_os != host_os or target_cpu != host_cpu
          is_cross_compiling = target_os != host_os || target_cpu != host_cpu
          EOFLLVM

          # 注意：不创建 proto_library.gni
          # 因为我们已经注释掉了 base/tracing/protos 的依赖
          # 不再需要 proto_library 模板

          echo "GN 配置文件创建完成"

      - name: 配置编译参数
        working-directory: src
        run: |
          echo "========================================="
          echo "配置编译参数"
          echo "========================================="

          # 鸿蒙使用 target_os="android" 因为 BUILDCONFIG.gn 不支持 ohos
          # 但是使用华为的 NDK 和 sysroot
          export EXTRA_FLAGS='target_os="android" target_cpu="${{ matrix.arch }}"'
          export TMPDIR="$PWD/tmp"
          mkdir -p "$TMPDIR"

          FLAGS='
            is_official_build=true
            exclude_unwind_tables=true
            enable_resource_allowlist_generation=false
            symbol_level=0

            is_clang=true
            use_sysroot=false

            fatal_linker_warnings=false
            treat_warnings_as_errors=false

            is_cronet_build=true
            chrome_pgo_phase=0

            enable_base_tracing=false
            use_perfetto_client_library=false
            use_udev=false
            use_aura=false
            use_ozone=false
            use_gio=false
            use_platform_icu_alternatives=true
            use_glib=false

            disable_file_support=true
            enable_websockets=false
            use_kerberos=false
            enable_mdns=false
            enable_reporting=false
            include_transport_security_state_preload_list=false
            enable_device_bound_sessions=false
            enable_bracketed_proxy_uris=true
            enable_quic_proxy_support=true

            use_nss_certs=false

            enable_backup_ref_ptr_support=false
            enable_dangling_raw_ptr_checks=false
            enable_shadow_metadata=false
          '

          FLAGS="$FLAGS
            default_min_sdk_version=24
            is_high_end_android=true
          "

          ./gn/out/gn gen "out/Release-ohos-${{ matrix.arch }}" --args="$FLAGS $EXTRA_FLAGS"

      - name: 编译共享库
        working-directory: src
        run: |
          echo "========================================="
          echo "开始编译 libnaive.so"
          echo "========================================="
          echo "配置: 隐藏警告, 发现10个错误后停止"
          echo ""

          ccache -z

          # 编译配置:
          # -k 10 = 最多允许10个错误,第11个错误时停止
          # 2>&1 = 合并标准输出和错误输出
          # grep -v "warning:" = 过滤掉所有警告信息
          # 保存完整输出到临时文件,同时过滤警告后显示
          ninja -C "out/Release-ohos-${{ matrix.arch }}" -k 10 naive_shared 2>&1 | tee full_build.log | grep -v "warning:"
          BUILD_STATUS=${PIPESTATUS[0]}

          # 如果编译失败,提取并显示详细的错误信息
          if [ $BUILD_STATUS -ne 0 ]; then
            echo ""
            echo "========================================="
            echo "🔴 编译失败 - 详细错误信息"
            echo "========================================="
            echo ""

            # 统计错误数量
            ERROR_COUNT=$(grep -c "error:" full_build.log || echo "0")
            echo "总错误数: $ERROR_COUNT"
            echo ""

            # 提取所有错误及其上下文(前后各5行,更详细)
            echo "========================================="
            echo "错误详情 (包含上下文):"
            echo "========================================="
            grep -B 5 -A 5 "error:" full_build.log | grep -v "warning:" | head -200

            echo ""
            echo "========================================="
            echo "错误摘要 (仅错误行):"
            echo "========================================="
            grep "error:" full_build.log

            echo ""
            echo "========================================="
            echo "💡 提示:"
            echo "  - 上面显示了所有错误及其上下文"
            echo "  - 错误数量: $ERROR_COUNT"
            echo "  - 请根据错误信息修复代码"
            echo "========================================="

            exit 1
          fi

          ccache -s

          echo ""
          echo "========================================="
          echo "编译完成"
          echo "========================================="

      - name: 检查编译产物
        working-directory: src
        run: |
          echo "========================================="
          echo "编译产物信息"
          echo "========================================="

          SO_FILE="out/Release-ohos-${{ matrix.arch }}/libnaive.so"

          if [ -f "$SO_FILE" ]; then
            echo "✅ 编译成功！"
            echo ""
            echo "文件信息:"
            ls -lh "$SO_FILE"
            echo ""
            echo "文件类型:"
            file "$SO_FILE"
            echo ""
            echo "依赖库:"
            readelf -d "$SO_FILE" | grep NEEDED || true
            echo ""
            echo "========================================="
            echo "导出符号 (前50个):"
            echo "========================================="
            nm -D "$SO_FILE" | grep " T \| W " | head -50 || readelf -s "$SO_FILE" | grep -E "FUNC.*GLOBAL" | head -50 || true
            echo ""
            echo "========================================="
            echo "查找 main 函数:"
            echo "========================================="
            nm -D "$SO_FILE" | grep -i main || echo "未找到 main 符号"
          else
            echo "❌ 编译失败：找不到 $SO_FILE"
            exit 1
          fi

      - name: 打包产物
        working-directory: src
        run: |
          BUNDLE="libnaive-ohos-${{ matrix.arch }}"
          mkdir -p "$BUNDLE"

          cp "out/Release-ohos-${{ matrix.arch }}/libnaive.so" "$BUNDLE/"
          cp config.json ../LICENSE ../USAGE.txt "$BUNDLE/" || true

          cat > "$BUNDLE/README.md" <<'EOFREADME'
          # NaiveProxy 鸿蒙共享库

          ## 文件说明

          - `libnaive.so` - NaiveProxy 核心库
          - `config.json` - 配置文件示例

          ## 使用方法

          ### 集成到鸿蒙应用

          1. 将 `libnaive.so` 复制到你的鸿蒙项目：
             ```
             YourHarmonyApp/entry/libs/arm64-v8a/libnaive.so
             ```

          2. 在 ArkTS 中调用：
             ```typescript
             import naive from 'libnaive.so';
             ```

          ## 架构

          - arm64-v8a: 鸿蒙手机（ARM64）
          - x86_64: 鸿蒙模拟器

          ## 许可证

          BSD License
          EOFREADME

          tar czf "$BUNDLE.tar.gz" "$BUNDLE"
          sha256sum "out/Release-ohos-${{ matrix.arch }}/libnaive.so" > sha256sum.txt
          echo "SHA256SUM=$(cut -d' ' -f1 sha256sum.txt)" >> $GITHUB_ENV

      - name: 上传编译产物
        uses: actions/upload-artifact@v4
        with:
          name: libnaive-ohos-${{ matrix.arch }}-sha256-${{ env.SHA256SUM }}
          path: |
            src/libnaive-ohos-${{ matrix.arch }}.tar.gz
            src/sha256sum.txt
          retention-days: 30

      - name: 上传到 Release
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            src/libnaive-ohos-${{ matrix.arch }}.tar.gz
            src/sha256sum.txt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  summary:
    name: 编译摘要
    needs: build-ohos-so
    runs-on: ubuntu-22.04
    if: always()

    steps:
      - name: 生成摘要
        run: |
          echo "## 🎉 NaiveProxy 鸿蒙版本编译完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📦 编译产物" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "在 **Artifacts** 部分下载编译好的 .so 文件" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📝 下一步" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "1. 下载 \`libnaive-ohos-arm64.tar.gz\`" >> $GITHUB_STEP_SUMMARY
          echo "2. 解压得到 \`libnaive.so\`" >> $GITHUB_STEP_SUMMARY
          echo "3. 集成到你的鸿蒙应用项目" >> $GITHUB_STEP_SUMMARY
          echo "4. 参考 README.md 进行开发" >> $GITHUB_STEP_SUMMARY
